# Originality Record

MarketProof source code was not created before the Build with CMC 2026 build window.

The repository itself was initialized at `2026-09-09T05:36:18.796Z`, after submissions opened at `2026-09-09T00:00:00Z`.

Pre-window Nymrel work was limited to research, concept definition, risk boundaries, acceptance criteria, and execution planning. Those materials are requirements/background only; no pre-window MarketProof source code was copied into this repository.

The following competition-specific work is new in-window work:

- CoinMarketCap keyless API adapter
- v3 response normalization
- strict stale/error/partial/mismatch rejection
- synthetic CMC fixture
- deterministic evidence and response digests
- live evidence capture CLI
- tests and acceptance evidence

Open-source dependencies are not required for the current implementation; it uses the Python standard library only.
